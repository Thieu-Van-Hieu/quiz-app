name: frontend
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  flutter_riverpod: ^2.6.1
  go_router: ^17.1.0
  hooks_riverpod: ^2.6.1
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.5
  riverpod_annotation: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  isar_generator: 3.1.0+1
  build_runner: ^2.4.0 # Dùng bản 2.4.x để ổn định hơn
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true

  # Để dùng các file hình ảnh/icon từ dự án cũ, bạn sẽ khai báo tại đây:
  # assets:
  #   - assets/images/

